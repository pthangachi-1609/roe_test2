name: Matrix Build with Artifacts

on:
  push:
    branches:
      - main

jobs:
  build-ubuntu:
    runs-on: ubuntu-latest
    steps:
      - name: matrix-02f2205
        run: echo "Building for ubuntu-latest"
      - name: Generate build artifact
        run: echo "Build output from ubuntu-latest" > output.txt
      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: build-02f2205-ubuntu-latest
          path: output.txt

  build-macos:
    runs-on: macos-latest
    steps:
      - name: matrix-02f2205
        run: echo "Building for macos-latest"
      - name: Generate build artifact
        run: echo "Build output from macos-latest" > output.txt
      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: build-02f2205-macos-latest
          path: output.txt

  build-windows:
    runs-on: windows-latest
    steps:
      - name: matrix-02f2205
        run: echo "Building for windows-latest"
      - name: Generate build artifact
        run: echo "Build output from windows-latest" > output.txt
      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: build-02f2205-windows-latest
          path: output.txt